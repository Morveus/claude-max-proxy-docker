apiVersion: v1
kind: Secret
metadata:
  name: claude-proxy-secrets
  namespace: claude-proxy
type: Opaque
stringData:
  # API key clients must send to authenticate with the proxy
  PROXY_API_KEY: "change-me-to-a-strong-secret"
