apiVersion: v1
kind: Service
metadata:
  name: claude-proxy
  namespace: claude-proxy
spec:
  selector:
    app: claude-proxy
  ports:
    - port: 3456
      targetPort: 3456
      protocol: TCP
      name: http
  type: ClusterIP
