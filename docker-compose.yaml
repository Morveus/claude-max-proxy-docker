services:
  claude-proxy:
    build: .
    ports:
      - "3456:3456"
    environment:
      - HOST=0.0.0.0
      - PORT=3456
      - CLAUDE_MODEL=sonnet
    volumes:
      - claude-config:/home/node/.claude
      - claude-dotconfig:/home/node/.config
      - ./workspace:/workspace
    restart: unless-stopped

volumes:
  claude-config:
  claude-dotconfig:
